{
  "name": "spartan-career-compass-report",
  "version": "1.0.0",
  "description": "Generates Report.docx from scripts/_build_report.js (run: npm install && npm run build-report)",
  "private": true,
  "scripts": {
    "build-report": "node scripts/_build_report.js"
  },
  "dependencies": {
    "docx": "^9.6.1"
  }
}
